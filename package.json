{
  "name": "advanced-screen-recorder",
  "version": "2.0.0",
  "description": "Professional Chrome extension for screen recording",
  "scripts": {
    "build": "echo 'No build step required for vanilla JS'",
    "lint": "eslint src/**/*.js",
    "format": "prettier --write src/**/*.{js,css,html}"
  },
  "keywords": [
    "chrome-extension",
    "screen-recorder",
    "video-capture"
  ],
  "author": "Declan",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.50.0",
    "prettier": "^3.0.3"
  }
}
